[tool.ruff]
target-version = "py310"
line-length = 88
extend-exclude = [
  "build",
  "install",
  "log",
  "logs",
  "cache",
  "research",
  "src/external",
]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
line-ending = "lf"

