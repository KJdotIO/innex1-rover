# Dev Session Notes — 2026-06-09

Point-in-time notes from the remote hardware test session on the Jetson.

## Summary

Pass 3 completed as a full hardware rehearsal with drivetrain, bucket-ladder
actuators, excavation BLDC, deposition actuators, Ouster LiDAR and front OAK-D
recorded into one evidence bag.

The lab team confirmed the visible motion looked good. The Jetson-side telemetry
ended healthy, with no drivetrain fault, no E-stop and no motion inhibit.

## Code Under Test

Both the Mac workspace and Jetson workspace were on `main` at:

```text
5b16a85 drivetrain: raise bench throttle cap
```

The important support changes in this test were:

- actuator and deposition actuator watchdogs fail closed when non-zero commands
  stop being refreshed
- excavation BLDC has a ROS-facing command and feedback path
- Xbox teleop publishes held actuator commands continuously
- drivetrain bench default throttle cap is `0.8` for visible Gimson motion

## Final Evidence Bag

The final Pass 3 bag is stored on the Jetson:

```text
/home/innex-1/innex1_test_data/pass3_20260609_151413_final_all_systems
```

Bag check:

```text
Bag size: 2.3 GiB
Duration: 150.485749387s
Messages: 73332
Storage: sqlite3
```

Recorded topics included:

- `/cmd_vel_safe`
- `/cmd_vel_gated`
- `/actuator/cmd`
- `/deposition/actuator/cmd`
- `/excavation/bldc/cmd`
- `/excavation/bldc/feedback`
- `/drivetrain/status`
- `/drivetrain/telemetry`
- `/joint_states_wheels`
- `/odom_wheels`
- `/ouster/points`
- `/ouster/scan`
- `/ouster/imu`
- `/ouster/metadata`
- `/ouster/telemetry`
- `/camera_front/rgb/image_raw/compressed`
- `/camera_front/stereo/image_raw/compressedDepth`
- `/camera_front/camera_info`
- `/camera_front/imu/data`
- `/camera_front/points`

The Ouster metadata JSON generated in the repo root was moved beside the bag:

```text
/home/innex-1/innex1_test_data/pass3_20260609_151413_final_all_systems/artifacts/os-122610007923-metadata.json
```

## Final Health Readback

Final drivetrain status after the forced stop:

```text
state: 1
fault_code: 0
estop_active: false
motion_inhibited: false
controller_online: [true, true]
wheel_velocity_rps: [0.0, 0.0, 0.0, 0.0]
encoder_ticks: [4346, 4639, 4578, 4586]
```

The wheel encoders moved from roughly `[-600, -650]` before the run to roughly
`[4.3k, 4.6k]` afterwards, confirming the Gimsons moved during the final pass.

The BLDC feedback count changed from `114847` to `117943`, confirming feedback
was recorded while the excavation motor was commanded.

## Cleanup State

Before shutdown, an explicit all-stop was sent to:

- `/cmd_vel_safe`
- `/actuator/cmd`
- `/deposition/actuator/cmd`
- `/excavation/bldc/cmd`

The drivetrain, Ouster, OAK-D, Foxglove and rosbridge processes were then stopped
on the Jetson. The Jetson repo was left clean on `main`, matching `origin/main`.

Disk state after the final bag:

```text
/dev/mmcblk0p1: 56G total, 46G used, 7.8G available, 86% used
```

